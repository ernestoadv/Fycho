/**
 * @packageDocumentation
 * Profile page.
 */
import Template from '../templates/profile';

/**
 * @returns {React.JSX.Element} A React element that renders the Profile Page.
 */
export default function Profile() {
  return <Template name="Profile" />;
}
